services:
  entity_reference_override_migrate.commands:
    class: Drupal\entity_reference_override\Commands\MigrateCommands
    arguments: ['@database', '@config.factory', '@keyvalue', '@entity_field.manager', '@entity.last_installed_schema.repository', '@entity_display.repository', '@plugin.manager.field.widget']
    tags:
      - { name: drush.command }
